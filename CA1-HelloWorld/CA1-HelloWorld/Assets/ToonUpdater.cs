using UnityEngine;

public class ToonUpdater : MonoBehaviour
{
    [SerializeField]
    private float outlineOffset = 0.01f;
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
